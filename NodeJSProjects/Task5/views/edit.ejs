<!DOCTYPE html>
<html>
<head>
  <title>Edit User</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="form-container">
    <h1>Edit User</h1>
    <form action="/update/<%= user.id %>" method="POST">
      <label>Name: <input type="text" name="username" value="<%= user.username %>" required></label>
      <label>Email: <input type="email" name="email" value="<%= user.email %>" required></label>
      <label>Age: <input type="number" name="age" value="<%= user.age %>" required></label>
      <label>Gender:
        <select name="gender" required>
          <option value="Male" <%= user.gender === 'Male' ? 'selected' : '' %>>Male</option>
          <option value="Female" <%= user.gender === 'Female' ? 'selected' : '' %>>Female</option>
          <option value="Other" <%= user.gender === 'Other' ? 'selected' : '' %>>Other</option>
        </select>
      </label>
      <label>Password: <input type="password" name="password" value="<%= user.password %>" required></label>
      <button type="submit">Update</button>
    </form>
  </div>
</body>
</html>
